import * as React from 'react'
import { TItems } from './todo'

export default function TodoExample({
    items,
    setItems,
}: {
    items:TItems;
    setItems: React.Dispatch<React.SetStateAction<TItems>>;
}){
    
}